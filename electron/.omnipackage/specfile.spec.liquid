Name:       omnipackage_example_electron
Version:    {{ version }}
Release:    1%{?dist}
Summary:    Example RPM Electron project with OmniPackage
License:    GPL-3.0-or-later
URL:        https://omnipackage.org/
Source0:    {{ source_folder_name }}.tar.gz

BuildRequires: {{ build_dependencies | join: ' ' }}
{% if runtime_dependencies.size > 0 %}
Requires: {{ runtime_dependencies | join: ', ' }}
{% endif %}

%define debug_package %{nil}
%define _build_id_links none

%description
Example RPM Electron project with OmniPackage


%prep
%setup -q


%build


%install
.omnipackage/install.sh %{buildroot}


%files
%attr(755, root, root) /usr/bin/omnipackage_example_electron
%attr(755, root, root) /usr/lib/omnipackage_example_electron/
%attr(644, root, root) /usr/share/applications/omnipackage_example_electron.desktop
%attr(644, root, root) /usr/share/icons/hicolor/32x32/apps/omnipackage_example_electron.png
%attr(644, root, root) /usr/share/icons/hicolor/128x128/apps/omnipackage_example_electron.png
%attr(644, root, root) /usr/share/icons/hicolor/256x256@2/apps/omnipackage_example_electron.png

%changelog
* Sun Feb 18 2024 Chuck Norris <chuck@norr.is>
- Chuck Norris does not use revision control software. None of his code has ever needed revision.
